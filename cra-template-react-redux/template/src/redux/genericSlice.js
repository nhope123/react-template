import { createSlice } from '@reduxjs/toolkit'

const initialState = { }

const genericSlice = createSlice({
    name: 'string',
    initialState,
    reducers: {  },
    extraReducers: {  }
})

export const {  } = genericSlice.actions
export default genericSlice.reducer